chainer==4.3.0
six==1.10.0
PyYAML==5.4
tqdm==4.14.0  # needed for train.py and test.py only                                                     
mlconf==0.0.1 # needed for train.py and test.py only
pynput==1.3.5 # needed for visualisation
                                          
